# config/database.ini
; database:
;   user: bioinfo_user
;   password: 123456
;   host: 192.168.100.13  # 如果测试节点与 MySQL 容器不在同一节点，替换为容器节点 IP
;   port: 3306
;   database: bioinfo_db

[database]
# 数据库连接信息（从环境变量获取，默认值用于开发环境）
host = ${DB_HOST:-192.168.100.13}          
port = ${DB_PORT:-3306}                
user = ${DB_USER:-root}                
password = ${DB_PASSWORD:-vklz123}          
database = ${DB_NAME:-bio_db}   
charset = utf8mb4