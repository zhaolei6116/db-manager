SQLAlchemy>=2.0.0,<3.0.0
PyYAML>=6.0,<7.0
APScheduler>=3.10.0,<4.0.0
pymysql>=1.1.0,<2.0.0
# logging 是内置模块，无需安装，但如果需要扩展（如rotating logs），可选添加：
# python-logging-handlers 不是标准；实际使用内置logging即可。
# 后续任务可能添加：pydantic>=2.0 (验证), pytest>=7.0 (测试)
# requests>=2.0 (HTTP请求), Flask>=2.0 (Web框架)
# 具体依赖根据项目需求调整 